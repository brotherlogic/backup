syntax = "proto3";

package backup;

message Config {
  repeated BackupSpec specs = 1;
}

message BackupSpec {
  string server = 1;
  string base_directory = 2;
  string match_regex = 3;
  string target_directory = 4;
}